<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <!--  sama  di padding-->

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/list_img_user"
        android:layout_width="55dp"
        android:layout_height="55dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:src="@android:color/darker_gray" />
    <!--  sama layout_width -->
    <TextView
        android:id="@+id/follow_txt_username"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="65dp"
        android:layout_marginTop="7dp"
        android:text="@string/username"
        android:textSize="16sp"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@+id/list_img_user"
        app:layout_constraintTop_toTopOf="parent" />
    <!--  sama di id, layout -->

    <TextView
        android:id="@+id/follow_txt_htmlUrl"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="65dp"
        android:layout_marginTop="7dp"
        android:ellipsize="end"
        android:maxLines="2"
        android:text="@string/html_url"
        app:layout_constraintStart_toEndOf="@+id/list_img_user"
        app:layout_constraintTop_toBottomOf="@+id/follow_txt_username" />
    <!--  sama id nya,text, maxlines -->

</androidx.constraintlayout.widget.ConstraintLayout>